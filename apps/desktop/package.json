{
  "name": "@margin/desktop",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "ios:init": "tauri ios init --ci",
    "ios:dev": "tauri ios dev",
    "ios:open": "tauri ios dev --open",
    "ios:build": "tauri ios build",
    "package:mac:app": "tauri build --bundles app --no-sign --ci",
    "package:mac:dmg": "node ../../scripts/package-mac-dmg.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.4"
  }
}
